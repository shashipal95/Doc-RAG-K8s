global:
  scrape_interval: 5s # Scrape metrics every 5 seconds
  evaluation_interval: 30s

scrape_configs:
  - job_name: 'docchat-backend'
    static_configs:
      - targets: ['backend:8000']

  - job_name: 'qdrant'
    static_configs:
      - targets: ['qdrant:6333']